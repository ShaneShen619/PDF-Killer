# PDF-Killer
A script-based utility that performs boolean text-matching across a PDF directory to identify page indices containing target strings, subsequently executing page-level extraction and concatenation into a new output stream.
